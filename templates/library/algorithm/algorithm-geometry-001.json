{
  "id": "algorithm-geometry-001",
  "category": "algorithm/geometry/convex_hull",
  "intents_zh": [
    "凸包",
    "convex hull",
    "Andrew monotone chain",
    "給我 convex hull"
  ],
  "intents_en": [
    "convex hull",
    "Andrew monotone chain",
    "convex hull 2d"
  ],
  "slots": [
    "fn_name"
  ],
  "code": "def {{fn_name}}(points):\n    pts = sorted(set(points))\n    if len(pts) <= 1: return pts\n    def cross(o, a, b): return (a[0]-o[0])*(b[1]-o[1]) - (a[1]-o[1])*(b[0]-o[0])\n    lower = []\n    for p in pts:\n        while len(lower) >= 2 and cross(lower[-2], lower[-1], p) <= 0:\n            lower.pop()\n        lower.append(p)\n    upper = []\n    for p in reversed(pts):\n        while len(upper) >= 2 and cross(upper[-2], upper[-1], p) <= 0:\n            upper.pop()\n        upper.append(p)\n    return lower[:-1] + upper[:-1]",
  "language": "py",
  "deps": [],
  "summary_zh": "Andrew 凸包 monotone chain"
}