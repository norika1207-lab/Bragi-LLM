{
  "id": "algorithm-ml-008",
  "category": "algorithm/ml/cosine_sim",
  "intents_zh": [
    "餘弦相似度",
    "cosine similarity",
    "向量相似度",
    "給我 cosine sim"
  ],
  "intents_en": [
    "cosine similarity",
    "vector similarity",
    "cosine sim numpy"
  ],
  "slots": [
    "fn_name"
  ],
  "code": "import numpy as np\n\ndef {{fn_name}}(a, b):\n    a = np.asarray(a, dtype=float)\n    b = np.asarray(b, dtype=float)\n    return float(a @ b / (np.linalg.norm(a) * np.linalg.norm(b) + 1e-12))",
  "language": "py",
  "deps": [
    "numpy"
  ],
  "summary_zh": "兩向量餘弦相似度"
}