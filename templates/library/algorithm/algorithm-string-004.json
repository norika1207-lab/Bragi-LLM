{
  "id": "algorithm-string-004",
  "category": "algorithm/string/manacher",
  "intents_zh": [
    "Manacher",
    "最長回文子字串",
    "給我 manacher"
  ],
  "intents_en": [
    "Manacher",
    "longest palindrome substring",
    "manacher algorithm"
  ],
  "slots": [
    "fn_name"
  ],
  "code": "def {{fn_name}}(s):\n    t = '#' + '#'.join(s) + '#'\n    n = len(t)\n    p = [0]*n\n    c = r = 0\n    for i in range(n):\n        mirror = 2*c - i\n        if i < r: p[i] = min(r-i, p[mirror])\n        while i+p[i]+1 < n and i-p[i]-1 >= 0 and t[i+p[i]+1] == t[i-p[i]-1]:\n            p[i] += 1\n        if i + p[i] > r:\n            c, r = i, i + p[i]\n    k = p.index(max(p))\n    return s[(k - p[k])//2 : (k + p[k])//2]",
  "language": "py",
  "deps": [],
  "summary_zh": "Manacher 最長回文子字串"
}