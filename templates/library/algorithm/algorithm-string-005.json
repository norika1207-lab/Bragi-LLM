{
  "id": "algorithm-string-005",
  "category": "algorithm/string/suffix_array",
  "intents_zh": [
    "後綴陣列",
    "suffix array",
    "給我 suffix array"
  ],
  "intents_en": [
    "suffix array",
    "build suffix array",
    "sa construction"
  ],
  "slots": [
    "fn_name"
  ],
  "code": "def {{fn_name}}(s):\n    n = len(s)\n    sa = list(range(n))\n    rank = [ord(c) for c in s]\n    tmp = [0]*n\n    k = 1\n    while True:\n        def key(i):\n            return (rank[i], rank[i+k] if i+k < n else -1)\n        sa.sort(key=key)\n        tmp[sa[0]] = 0\n        for i in range(1, n):\n            tmp[sa[i]] = tmp[sa[i-1]] + (1 if key(sa[i]) != key(sa[i-1]) else 0)\n        rank = tmp[:]\n        if rank[sa[-1]] == n - 1: break\n        k *= 2\n    return sa",
  "language": "py",
  "deps": [],
  "summary_zh": "O(n log^2 n) 後綴陣列"
}