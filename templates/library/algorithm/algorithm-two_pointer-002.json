{
  "id": "algorithm-two_pointer-002",
  "category": "algorithm/two_pointer/remove_dup",
  "intents_zh": [
    "移除重複元素",
    "原地去重 sorted",
    "給我 remove duplicates"
  ],
  "intents_en": [
    "remove duplicates sorted",
    "in place dedup",
    "two pointer dedup"
  ],
  "slots": [
    "fn_name"
  ],
  "code": "def {{fn_name}}(arr):\n    if not arr: return 0\n    k = 1\n    for i in range(1, len(arr)):\n        if arr[i] != arr[i-1]:\n            arr[k] = arr[i]\n            k += 1\n    return k",
  "language": "py",
  "deps": [],
  "summary_zh": "原地移除有序陣列重複，回新長度"
}