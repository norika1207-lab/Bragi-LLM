{
  "id": "depth-algorithm-028-z-function",
  "category": "algorithm/string/z",
  "intents_zh": [
    "Z 函數",
    "Z algorithm",
    "字串 Z 數組",
    "前綴匹配長度",
    "Z 函數模板",
    "linear Z function",
    "z box 算法"
  ],
  "intents_en": [
    "Z function",
    "Z algorithm",
    "Z array string",
    "prefix match length",
    "Z function template",
    "linear Z function",
    "z box algorithm"
  ],
  "slots": [
    "s"
  ],
  "code": "def z_function(s={{s}}):\n    n = len(s); z = [0]*n; l = r = 0\n    for i in range(1, n):\n        if i < r:\n            z[i] = min(r-i, z[i-l])\n        while i+z[i] < n and s[z[i]] == s[i+z[i]]:\n            z[i] += 1\n        if i+z[i] > r:\n            l, r = i, i+z[i]\n    return z",
  "language": "py",
  "deps": [],
  "summary_zh": "Z 函數計算每位置的最長前綴匹配"
}