{
  "id": "depth-algorithm-029-manacher",
  "category": "algorithm/string/manacher",
  "intents_zh": [
    "Manacher 算法",
    "最長迴文子串",
    "O(n) 迴文",
    "manacher 模板",
    "palindrome linear",
    "迴文半徑數組",
    "回文子串"
  ],
  "intents_en": [
    "Manacher algorithm",
    "longest palindrome substring",
    "linear palindrome",
    "manacher template",
    "palindrome radius array",
    "O(n) palindrome",
    "longest palindromic substring"
  ],
  "slots": [
    "s"
  ],
  "code": "def manacher(s={{s}}):\n    t = '#' + '#'.join(s) + '#'\n    n = len(t); p = [0]*n; c = r = 0\n    for i in range(n):\n        mirror = 2*c - i\n        if i < r: p[i] = min(r-i, p[mirror])\n        while i+p[i]+1 < n and i-p[i]-1 >= 0 and t[i+p[i]+1] == t[i-p[i]-1]:\n            p[i] += 1\n        if i + p[i] > r:\n            c, r = i, i+p[i]\n    k = p.index(max(p))\n    start = (k - p[k]) // 2\n    return s[start:start+p[k]]",
  "language": "py",
  "deps": [],
  "summary_zh": "Manacher 線性算法求最長迴文子串"
}