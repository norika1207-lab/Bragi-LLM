{
  "id": "depth-algorithm-047-sos-dp",
  "category": "algorithm/dp/sos",
  "intents_zh": [
    "子集和 DP",
    "SOS DP",
    "sum over subsets",
    "子集求和 DP",
    "高維前綴和",
    "子集枚舉 DP",
    "SOS 模板"
  ],
  "intents_en": [
    "sum over subsets DP",
    "SOS DP",
    "subset sum DP",
    "high dimensional prefix sum",
    "subset enumeration DP",
    "SOS template",
    "mobius subset"
  ],
  "slots": [
    "arr",
    "n_bits"
  ],
  "code": "def sos_dp(a={{arr}}, n={{n_bits}}):\n    dp = a[:]\n    for i in range(n):\n        for mask in range(1<<n):\n            if mask & (1<<i):\n                dp[mask] += dp[mask ^ (1<<i)]\n    return dp",
  "language": "py",
  "deps": [],
  "summary_zh": "SOS DP 計算所有子集和 O(n 2^n)"
}