{
  "id": "database-devops-050-nginx-rate-limit",
  "category": "database-devops/nginx/security",
  "intents_zh": [
    "nginx 限流",
    "limit_req 範本",
    "API rate limit"
  ],
  "intents_en": [
    "nginx rate limit",
    "limit_req config",
    "api throttle"
  ],
  "slots": [
    "zone_name",
    "rate"
  ],
  "code": "limit_req_zone $binary_remote_addr zone={{zone_name}}:10m rate={{rate}};\n\nserver {\n  listen 80;\n  location /api/ {\n    limit_req zone={{zone_name}} burst=20 nodelay;\n    limit_req_status 429;\n    proxy_pass http://127.0.0.1:3000;\n  }\n}",
  "language": "nginx",
  "deps": [],
  "summary_zh": "nginx 用 limit_req 做 API 限流"
}