{
  "id": "database-devops-058-gha-docker-build-push",
  "category": "database-devops/cicd/github-actions",
  "intents_zh": [
    "GHA build docker image 推 GHCR",
    "CI 推 docker image",
    "build-push-action"
  ],
  "intents_en": [
    "github actions docker build push",
    "push to ghcr",
    "build-push-action"
  ],
  "slots": [
    "image_name"
  ],
  "code": "name: Docker\non:\n  push: { branches: [main], tags: ['v*'] }\n\njobs:\n  build:\n    runs-on: ubuntu-latest\n    permissions:\n      contents: read\n      packages: write\n    steps:\n      - uses: actions/checkout@v4\n      - uses: docker/setup-buildx-action@v3\n      - uses: docker/login-action@v3\n        with:\n          registry: ghcr.io\n          username: ${{ github.actor }}\n          password: ${{ secrets.GITHUB_TOKEN }}\n      - uses: docker/metadata-action@v5\n        id: meta\n        with: { images: ghcr.io/${{ github.repository_owner }}/{{image_name}} }\n      - uses: docker/build-push-action@v5\n        with:\n          push: true\n          tags: ${{ steps.meta.outputs.tags }}\n          cache-from: type=gha\n          cache-to: type=gha,mode=max",
  "language": "yaml",
  "deps": [],
  "summary_zh": "GHA build + push docker 到 GHCR"
}