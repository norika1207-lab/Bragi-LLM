{
  "id": "depth-database-devops-022-vagrant-dev-vm",
  "category": "database-devops/vagrant",
  "intents_zh": [
    "Vagrant 開發機",
    "vagrantfile 範例",
    "本機開發 VM"
  ],
  "intents_en": [
    "Vagrant dev VM",
    "Vagrantfile example",
    "local dev virtual machine"
  ],
  "slots": [
    "box",
    "memory_mb",
    "forward_port"
  ],
  "code": "Vagrant.configure('2') do |config|\n  config.vm.box = '{{box}}'\n  config.vm.network 'forwarded_port', guest: 80, host: {{forward_port}}\n  config.vm.synced_folder '.', '/vagrant'\n  config.vm.provider 'virtualbox' do |vb|\n    vb.memory = {{memory_mb}}\n    vb.cpus = 2\n  end\n  config.vm.provision 'shell', inline: <<-SHELL\n    apt-get update && apt-get install -y nginx\n  SHELL\nend",
  "language": "ruby",
  "deps": [
    "vagrant",
    "virtualbox"
  ],
  "summary_zh": "Vagrantfile 開本機 dev VM 並裝 nginx"
}