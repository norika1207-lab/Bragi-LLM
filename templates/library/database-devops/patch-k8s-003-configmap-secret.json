{
  "id": "patch-k8s-003-configmap-secret",
  "category": "database-devops/kubernetes/config",
  "intents_zh": [
    "產生 ConfigMap 跟 Secret 模板",
    "k8s 設定檔跟敏感資料分開",
    "configmap 跟 secret 一起寫",
    "我要 env 注入用的 cm 跟 secret",
    "幫我做 k8s 設定 yaml"
  ],
  "intents_en": [
    "create configmap and secret yaml",
    "k8s configmap with secret template",
    "env config and secret manifest",
    "generate cm plus secret for app",
    "kubernetes config injection"
  ],
  "slots": [
    "cm_name",
    "secret_name",
    "namespace",
    "config_key",
    "config_value",
    "secret_key",
    "secret_value_base64"
  ],
  "code": "apiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{cm_name}}\n  namespace: {{namespace}}\ndata:\n  {{config_key}}: \"{{config_value}}\"\n---\napiVersion: v1\nkind: Secret\nmetadata:\n  name: {{secret_name}}\n  namespace: {{namespace}}\ntype: Opaque\ndata:\n  {{secret_key}}: {{secret_value_base64}}\n",
  "language": "yaml",
  "deps": [
    "kubernetes"
  ],
  "summary_zh": "成對的 ConfigMap 加 Opaque Secret 模板,Secret value 需先 base64 編碼"
}