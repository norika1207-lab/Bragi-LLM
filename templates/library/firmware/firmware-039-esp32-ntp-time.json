{
  "id": "firmware-039-esp32-ntp-time",
  "category": "firmware/esp32/time",
  "intents_zh": [
    "ESP32 NTP 對時",
    "網路時間同步",
    "SNTP 範例",
    "抓網路時間",
    "ESP32 校時"
  ],
  "intents_en": [
    "ESP32 NTP time sync",
    "get time from internet",
    "SNTP ESP32",
    "sync clock wifi",
    "configTime example"
  ],
  "slots": [
    "ssid",
    "password",
    "tz_offset"
  ],
  "code": "#include <WiFi.h>\n#include <time.h>\n\nvoid setup() {\n  Serial.begin(115200);\n  WiFi.begin(\"{{ssid}}\", \"{{password}}\");\n  while (WiFi.status() != WL_CONNECTED) delay(300);\n  configTime({{tz_offset}}, 0, \"pool.ntp.org\");\n  struct tm tm;\n  while (!getLocalTime(&tm)) delay(500);\n  Serial.printf(\"%04d-%02d-%02d %02d:%02d:%02d\\n\", tm.tm_year+1900, tm.tm_mon+1, tm.tm_mday, tm.tm_hour, tm.tm_min, tm.tm_sec);\n}\n\nvoid loop() {}",
  "language": "ino",
  "deps": [],
  "summary_zh": "ESP32 透過 NTP 取得網路時間"
}