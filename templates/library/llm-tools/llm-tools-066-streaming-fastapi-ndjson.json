{
  "id": "llm-tools-066-streaming-fastapi-ndjson",
  "category": "llm-tools/server/ndjson-stream",
  "intents_zh": [
    "ndjson 串流回應",
    "fastapi ndjson",
    "逐行 json 串流",
    "stream llm via ndjson",
    "ndjson endpoint",
    "行式 json 流",
    "ndjson chat",
    "jsonl stream"
  ],
  "intents_en": [
    "ndjson streaming response",
    "fastapi ndjson",
    "line-delimited json stream",
    "stream llm ndjson",
    "ndjson endpoint",
    "jsonl stream chat",
    "jsonl over http",
    "streaming json lines"
  ],
  "slots": [
    "model_name",
    "port"
  ],
  "code": "from fastapi import FastAPI\nfrom fastapi.responses import StreamingResponse\nfrom openai import OpenAI\nimport json, uvicorn\n\napp = FastAPI()\nclient = OpenAI()\n\ndef gen(prompt: str):\n    s = client.chat.completions.create(\n        model=\"{{model_name}}\",\n        messages=[{\"role\": \"user\", \"content\": prompt}],\n        stream=True,\n    )\n    for chunk in s:\n        d = chunk.choices[0].delta.content\n        if d:\n            yield json.dumps({\"delta\": d}) + \"\\n\"\n\n@app.get(\"/ndjson\")\ndef route(q: str):\n    return StreamingResponse(gen(q), media_type=\"application/x-ndjson\")\n\nif __name__ == \"__main__\":\n    uvicorn.run(app, host=\"0.0.0.0\", port={{port}})",
  "language": "py",
  "deps": [
    "fastapi",
    "uvicorn",
    "openai"
  ],
  "summary_zh": "FastAPI ndjson 行式 JSON 串流"
}