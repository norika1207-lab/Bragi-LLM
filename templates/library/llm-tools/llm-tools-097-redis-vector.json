{
  "id": "llm-tools-097-redis-vector",
  "category": "llm-tools/vectordb/redis",
  "intents_zh": [
    "redis 向量搜尋",
    "redis vector index",
    "redisearch vector",
    "用 redis 存 embedding",
    "redis HNSW",
    "redis vss",
    "redis vector example",
    "redis 向量"
  ],
  "intents_en": [
    "redis vector search",
    "redis vss",
    "redisearch vector index",
    "redis embedding store",
    "redis hnsw",
    "redis vector example",
    "minimal redis vector",
    "redis as vectordb"
  ],
  "slots": [
    "index_name",
    "dim"
  ],
  "code": "import redis\nfrom redis.commands.search.field import VectorField, TextField\nfrom redis.commands.search.indexDefinition import IndexDefinition, IndexType\n\nr = redis.Redis()\n\ntry:\n    r.ft(\"{{index_name}}\").create_index(\n        fields=[\n            TextField(\"text\"),\n            VectorField(\"vec\", \"HNSW\", {\"TYPE\": \"FLOAT32\", \"DIM\": {{dim}}, \"DISTANCE_METRIC\": \"COSINE\"}),\n        ],\n        definition=IndexDefinition(prefix=[\"doc:\"], index_type=IndexType.HASH),\n    )\nexcept redis.ResponseError:\n    pass\n\ndef add(id_: str, text: str, vec_bytes: bytes):\n    r.hset(f\"doc:{id_}\", mapping={\"text\": text, \"vec\": vec_bytes})",
  "language": "py",
  "deps": [
    "redis"
  ],
  "summary_zh": "Redis HNSW 向量索引建立"
}