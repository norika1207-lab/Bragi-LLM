{
  "id": "system-cli-082-tempdir-context",
  "category": "system-cli/file/temp",
  "intents_zh": [
    "python 暫存資料夾",
    "tempfile context",
    "用完自動清掉"
  ],
  "intents_en": [
    "python tempdir context",
    "auto cleanup temp folder",
    "tempfile TemporaryDirectory"
  ],
  "slots": [
    "work_fn"
  ],
  "code": "import tempfile, shutil\nfrom pathlib import Path\n\ndef {{work_fn}}(workdir: Path):\n    (workdir / 'data.txt').write_text('hello')\n    return list(workdir.iterdir())\n\nwith tempfile.TemporaryDirectory(prefix='job-') as td:\n    out = {{work_fn}}(Path(td))\n    print('produced', out)\n    # td auto-removed",
  "language": "py",
  "deps": [],
  "summary_zh": "自動清理 tempdir"
}