{
  "id": "web-backend-043-flask-pdf",
  "category": "web-backend/flask/export",
  "intents_zh": [
    "flask 產 PDF",
    "reportlab 生 PDF",
    "flask 下載 pdf",
    "PDF endpoint python",
    "invoice pdf flask"
  ],
  "intents_en": [
    "flask pdf generation",
    "reportlab pdf flask",
    "download pdf flask",
    "pdf endpoint python",
    "invoice pdf flask"
  ],
  "slots": [
    "filename",
    "title"
  ],
  "code": "from flask import send_file\nfrom reportlab.pdfgen import canvas\nimport io\n\ndef make_pdf():\n    buf = io.BytesIO()\n    c = canvas.Canvas(buf)\n    c.setFont('Helvetica-Bold', 18)\n    c.drawString(100, 800, '{{title}}')\n    c.setFont('Helvetica', 11)\n    c.drawString(100, 780, 'generated by flask')\n    c.showPage()\n    c.save()\n    buf.seek(0)\n    return send_file(buf, mimetype='application/pdf', as_attachment=True, download_name='{{filename}}.pdf')",
  "language": "py",
  "deps": [
    "flask",
    "reportlab"
  ],
  "summary_zh": "Flask ReportLab PDF 生成"
}