{
  "id": "depth-web-frontend-002-react-query-mutation",
  "category": "web-frontend/data-fetching/react-query",
  "intents_zh": [
    "react-query mutation",
    "送出表單後重抓",
    "useMutation 範例",
    "invalidateQueries 更新快取",
    "樂觀更新"
  ],
  "intents_en": [
    "react-query useMutation",
    "mutation invalidate queries",
    "optimistic update",
    "post then refetch",
    "tanstack mutation"
  ],
  "slots": [
    "mutation_key",
    "endpoint",
    "query_key"
  ],
  "code": "import { useMutation, useQueryClient } from '@tanstack/react-query';\n\nexport function use{{mutation_key}}() {\n  const qc = useQueryClient();\n  return useMutation({\n    mutationFn: async (payload: any) => {\n      const res = await fetch('{{endpoint}}', { method: 'POST', body: JSON.stringify(payload) });\n      return res.json();\n    },\n    onSuccess: () => qc.invalidateQueries({ queryKey: ['{{query_key}}'] }),\n  });\n}",
  "language": "tsx",
  "deps": [
    "@tanstack/react-query"
  ],
  "summary_zh": "useMutation 寫入並失效快取"
}